<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- aos_data_filter[AosDataFilter]SQL自动映射 -->
<!--  注意:此文件由AOS平台自动生成-禁止手工修改 2017-11-25 17:21:38 -->
<mapper namespace="aos.system.dao.AosDataFilterDao">
	
	<!-- 插入一个数据持久化对象(插入字段为传入PO实体的非空属性) -->
	<insert id="insert"  parameterType="AosDataFilterPO">
		INSERT INTO aos_data_filter (
		<if test="sqlid != null and sqlid != ''">
	         sqlid, <!-- 模块编码 -->
		</if>
		<if test="path != null and path != ''">
	         path, <!-- 过滤路径 -->
		</if>
		<if test="name != null and name != ''">
	         name, <!-- 过滤路径名称 -->
		</if>
		<if test="remark != null and remark != ''">
	         remark, <!-- 提示 -->
		</if>
		<if test="id != null">
	          id  <!-- 路径编码 -->
	   </if>
		)VALUES(
		<if test="sqlid != null and sqlid != ''">
	          #{sqlid, jdbcType=VARCHAR}, <!-- 模块编码 -->
	    </if>
		<if test="path != null and path != ''">
	          #{path, jdbcType=VARCHAR}, <!-- 过滤路径 -->
	    </if>
		<if test="name != null and name != ''">
	          #{name, jdbcType=VARCHAR}, <!-- 过滤路径名称 -->
	    </if>
		<if test="remark != null and remark != ''">
	          #{remark, jdbcType=VARCHAR}, <!-- 提示 -->
	    </if>
		<if test="id != null">
		     #{id, jdbcType=INTEGER}  <!-- 路径编码 -->
	    </if>
		)
	</insert>
	
	<!-- 插入一个数据持久化对象(含所有字段) -->
	<insert id="insertAll"  parameterType="AosDataFilterPO">
		INSERT INTO aos_data_filter (
		    <include refid="column" />
		)
		VALUES (
		    #{id, jdbcType=INTEGER},  <!-- 路径编码 -->
		    #{sqlid, jdbcType=VARCHAR},  <!-- 模块编码 -->
		    #{path, jdbcType=VARCHAR},  <!-- 过滤路径 -->
		    #{name, jdbcType=VARCHAR},  <!-- 过滤路径名称 -->
		    #{remark, jdbcType=VARCHAR}  <!-- 提示 -->
		)
	</insert>

	<!-- 根据主键修改数据持久化对象 -->
	<update id="updateByKey" parameterType="AosDataFilterPO">
		UPDATE aos_data_filter
		<set>
			<if test="id != null">
		          id = #{id, jdbcType=INTEGER},  <!-- 路径编码 -->
			</if>
			<if test="sqlid != null">
		          sqlid = #{sqlid, jdbcType=VARCHAR},  <!-- 模块编码 -->
			</if>
			<if test="path != null">
		          path = #{path, jdbcType=VARCHAR},  <!-- 过滤路径 -->
			</if>
			<if test="name != null">
		          name = #{name, jdbcType=VARCHAR},  <!-- 过滤路径名称 -->
			</if>
			<if test="remark != null">
		          remark = #{remark, jdbcType=VARCHAR},  <!-- 提示 -->
			</if>
		</set>
		WHERE  id = #{id}
	</update>
	
	<!-- 根据主键删除数据持久化对象 -->
	<delete id="deleteByKey">
		DELETE FROM aos_data_filter WHERE  id = #{id}
	</delete>

	<!-- 根据主键查询并返回数据持久化对象 -->
	<select id="selectByKey" resultType="AosDataFilterPO">
		SELECT
		<include refid="column" />
		FROM aos_data_filter WHERE  id = #{id}
	</select>

	<!-- 根据唯一组合条件查询并返回数据持久化对象 -->
	<select id="selectOne" parameterType="Dto" resultType="AosDataFilterPO">
		SELECT
		    <include refid="column" />
		FROM aos_data_filter
		<where>
		    <include refid="equal" />
		</where>		
	</select>

	<!-- 根据Dto查询并返回数据持久化对象集合 -->
	<select id="list" parameterType="Dto" resultType="AosDataFilterPO">
		SELECT
			<include refid="column" />	
		FROM aos_data_filter
		<where>
		    <include refid="equal" />
		</where>	
	</select>

	<!-- 根据Dto查询并返回分页数据持久化对象集合 -->
	<select id="listPage" parameterType="Dto" resultType="AosDataFilterPO" useCache="false">
	    SELECT
			<include refid="column" />	
		FRoM aos_data_filter
		<where>
		    <include refid="equal" />
		</where>	
	</select>
	
	<!-- 根据Dto模糊查询并返回数据持久化对象集合(字符型字段模糊匹配，其余字段精确匹配)并集 -->
	<select id="like" parameterType="Dto" resultType="AosDataFilterPO">
		SELECT
			<include refid="column" />	
		FROM aos_data_filter
		<where>
		    <include refid="like" />
		</where>	
	</select>
	<!-- 根据Dto模糊查询并返回数据持久化对象集合(字符型字段模糊匹配，其余字段精确匹配)交集 -->
	<select id="likeOr" parameterType="Dto" resultType="AosDataFilterPO">
		SELECT
			<include refid="column" />	
		FROM aos_data_filter
		<where>
		    <include refid="likeOr" />
		</where>	
	</select>

	<!-- 根据Dto模糊查询并返回分页数据持久化对象集合(字符型字段模糊匹配，其余字段精确匹配)交集 -->
	<select id="likePage" parameterType="Dto" resultType="AosDataFilterPO" useCache="false">
	    SELECT
			<include refid="column" />	
		FRoM aos_data_filter
		<where>
		    <include refid="like" />
		</where>	
	</select>
	<!-- 根据Dto模糊查询并返回分页数据持久化对象集合(字符型字段模糊匹配，其余字段精确匹配)并集 -->
	<select id="likeOrPage" parameterType="Dto" resultType="AosDataFilterPO" useCache="false">
	    SELECT
			<include refid="column" />	
		FRoM aos_data_filter
		<where>
		    <include refid="likeOr" />
		</where>	
	</select>	
	
	<!-- 根据Dto统计行数 -->
	<select id="rows" resultType="Integer" parameterType="Dto">
		SELECT IFNULL(COUNT(1),0) AS aos_rows_ FROM aos_data_filter
		<where>
		    <include refid="equal" />
		</where>
	</select>
	
	<!-- 根据数学表达式进行数学运算 -->
	<select id="calc" parameterType="Dto" resultType="String">
		SELECT
			${_expr}
		FROM aos_data_filter
		<where>
		    <include refid="equal" />
		</where>
	</select>
	
	<!-- 表字段 -->
	<sql id="column">
		  id,  <!-- 路径编码 -->
		  sqlid,  <!-- 模块编码 -->
		  path,  <!-- 过滤路径 -->
		  name,  <!-- 过滤路径名称 -->
		  remark  <!-- 提示 -->
	</sql>
	
	<!-- 表字段(用于外表的关联时引用) -->
	<sql id="column2">
		  aos_data_filter.id,  <!-- 路径编码 -->
		  aos_data_filter.sqlid,  <!-- 模块编码 -->
		  aos_data_filter.path,  <!-- 过滤路径 -->
		  aos_data_filter.name,  <!-- 过滤路径名称 -->
		  aos_data_filter.remark  <!-- 提示 -->
	</sql>

	<!-- Where精确匹配字段 -->
	<sql id="equal">
	   <if test="id != null ">
		      AND id = #{id}  <!-- 路径编码 -->
		</if>
	    <if test="sqlid != null and sqlid != ''">
		      AND sqlid = #{sqlid}  <!-- 模块编码 -->
		</if>
	    <if test="path != null and path != ''">
		      AND path = #{path}  <!-- 过滤路径 -->
		</if>
	    <if test="name != null and name != ''">
		      AND name = #{name}  <!-- 过滤路径名称 -->
		</if>
	    <if test="remark != null and remark != ''">
		      AND remark = #{remark}  <!-- 提示 -->
		</if>
	</sql>
	
	<!-- Where模糊匹配字段 交集-->
	<sql id="like">
	    <if test="sqlid != null and sqlid != ''">
		      AND sqlid like '${sqlid}%'  <!-- 模块编码 -->
		</if>
	    <if test="path != null and path != ''">
		      AND path like '${path}%'  <!-- 过滤路径 -->
		</if>
	    <if test="name != null and name != ''">
		      AND name like '${name}%'  <!-- 过滤路径名称 -->
		</if>
	    <if test="remark != null and remark != ''">
		      AND remark like '${remark}%'  <!-- 提示 -->
		</if>
	</sql>
	<!-- Where模糊匹配字段 并集 -->
	<sql id="likeOr">
	    <if test="sqlid != null and sqlid != ''">
		      Or sqlid like '${sqlid}%'  <!-- 模块编码 -->
		</if>
	    <if test="path != null and path != ''">
		      Or path like '${path}%'  <!-- 过滤路径 -->
		</if>
	    <if test="name != null and name != ''">
		      Or name like '${name}%'  <!-- 过滤路径名称 -->
		</if>
	    <if test="remark != null and remark != ''">
		      Or remark like '${remark}%'  <!-- 提示 -->
		</if>
	</sql>
	
</mapper>