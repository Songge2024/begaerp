<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- bs_proj_code_version[ProjCodeVersion]SQL自动映射 -->
<!--  注意:此文件由AOS平台自动生成-禁止手工修改 2019-03-08 13:39:11 -->
<mapper namespace="com.bl3.pm.basic.dao.ProjCodeVersionDao">
	
	<!-- 插入一个数据持久化对象(插入字段为传入PO实体的非空属性) -->
	<insert id="insert" useGeneratedKeys="true" keyProperty="code_version_id" parameterType="ProjCodeVersionPO">
		INSERT INTO bs_proj_code_version (
		<if test="code_version_number != null and code_version_number != ''">
	         code_version_number, <!-- 代码版本号 -->
		</if>
		<if test="version_id != null">
	         version_id, <!-- 项目版本号ID -->
		</if>
		<if test="test_version_id != null">
			 test_version_id,<!-- 测试版本号ID -->
		</if>
		<if test="proj_id != null">
	         proj_id, <!-- 项目ID -->
		</if>
		<if test="remark != null and remark != ''">
	         remark, <!-- 备注 -->
		</if>
		<if test="sortno != null">
	         sortno, <!-- 排序号 -->
		</if>
		<if test="sortno == null">
		 	sortno, <!-- 排序号 -->
		</if>
		<if test="create_id != null">
	         create_id, <!-- 创建人ID -->
		</if>
		<if test="create_time != null and create_time != ''">
	         create_time, <!-- 创建时间 -->
		</if>
		<if test="update_id != null">
	         update_id, <!-- 修改人ID -->
		</if>
		<if test="update_time != null and update_time != ''">
	         update_time, <!-- 修改时间 -->
		</if>
		<if test="state != null and state != ''">
	         state, <!-- 状态（1 有效 0 无效） -->
		</if>
	          code_version_id
		)VALUES(
		<if test="code_version_number != null and code_version_number != ''">
	          #{code_version_number, jdbcType=VARCHAR}, <!-- 代码版本号 -->
	    </if>
		<if test="version_id != null">
	          #{version_id, jdbcType=INTEGER}, <!-- 项目版本号ID -->
	    </if>
	    <if test="test_version_id != null">
	    	  #{test_version_id, jdbcType=INTEGER}, <!-- 测试版本号 -->
	    </if>
		<if test="proj_id != null">
	          #{proj_id, jdbcType=INTEGER}, <!-- 项目ID -->
	    </if>
		<if test="remark != null and remark != ''">
	          #{remark, jdbcType=VARCHAR}, <!-- 备注 -->
	    </if>
		<if test="sortno != null">
	          #{sortno, jdbcType=INTEGER}, <!-- 排序号 -->
	    </if>
	     <if test="sortno == null">
		 (SELECT ifnull(max(sortno),0) FROM bs_proj_code_version a WHERE state != -1 AND test_version_id = #{test_version_id})+1,
		 </if>
		<if test="create_id != null">
	          #{create_id, jdbcType=INTEGER}, <!-- 创建人ID -->
	    </if>
		<if test="create_time != null and create_time != ''">
	          #{create_time, jdbcType=VARCHAR}, <!-- 创建时间 -->
	    </if>
		<if test="update_id != null">
	          #{update_id, jdbcType=INTEGER}, <!-- 修改人ID -->
	    </if>
		<if test="update_time != null and update_time != ''">
	          #{update_time, jdbcType=VARCHAR}, <!-- 修改时间 -->
	    </if>
		<if test="state != null and state != ''">
	          #{state, jdbcType=VARCHAR}, <!-- 状态（1 有效 0 无效） -->
	    </if>
	          null
		)
	</insert>
	
	<!-- 插入一个数据持久化对象(含所有字段) -->
	<insert id="insertAll" useGeneratedKeys="true" keyProperty="code_version_id" parameterType="ProjCodeVersionPO">
		INSERT INTO bs_proj_code_version (
		    <include refid="column" />
		)
		VALUES (
		    #{code_version_id, jdbcType=INTEGER},  <!-- 代码版本号主键ID -->
		    #{code_version_number, jdbcType=VARCHAR},  <!-- 代码版本号 -->
		    #{version_id, jdbcType=INTEGER},  <!-- 版本号ID -->
		    #{test_version_id, jdbcType=INTEGER},  <!-- 测试版本号ID -->
		    #{proj_id, jdbcType=INTEGER},  <!-- 项目ID -->
		    #{remark, jdbcType=VARCHAR},  <!-- 备注 -->
		    #{sortno, jdbcType=INTEGER},  <!-- 排序号 -->
		    #{create_id, jdbcType=INTEGER},  <!-- 创建人ID -->
		    #{create_time, jdbcType=VARCHAR},  <!-- 创建时间 -->
		    #{update_id, jdbcType=INTEGER},  <!-- 修改人ID -->
		    #{update_time, jdbcType=VARCHAR},  <!-- 修改时间 -->
		    #{state, jdbcType=VARCHAR}  <!-- 状态（1 有效 0 无效） -->
		)
	</insert>

	<!-- 根据主键修改数据持久化对象 -->
	<update id="updateByKey" parameterType="ProjCodeVersionPO">
		UPDATE bs_proj_code_version
		<set>
			<if test="code_version_id != null">
		          code_version_id = #{code_version_id, jdbcType=INTEGER},  <!-- 代码版本号主键ID -->
			</if>
			<if test="code_version_number != null">
		          code_version_number = #{code_version_number, jdbcType=VARCHAR},  <!-- 代码版本号 -->
			</if>
			<if test="version_id != null">
		          version_id = #{version_id, jdbcType=INTEGER},  <!-- 版本号ID -->
			</if>
			<if test="test_version_id != null">
		          test_version_id = #{test_version_id, jdbcType=INTEGER},  <!-- 测试版本号ID -->
			</if>
			<if test="proj_id != null">
		          proj_id = #{proj_id, jdbcType=INTEGER},  <!-- 项目ID -->
			</if>
			<if test="remark != null">
		          remark = #{remark, jdbcType=VARCHAR},  <!-- 备注 -->
			</if>
			<if test="sortno != null">
		          sortno = #{sortno, jdbcType=INTEGER},  <!-- 排序号 -->
			</if>
			<if test="create_id != null">
		          create_id = #{create_id, jdbcType=INTEGER},  <!-- 创建人ID -->
			</if>
			<if test="create_time != null">
		          create_time = #{create_time, jdbcType=VARCHAR},  <!-- 创建时间 -->
			</if>
			<if test="update_id != null">
		          update_id = #{update_id, jdbcType=INTEGER},  <!-- 修改人ID -->
			</if>
			<if test="update_time != null">
		          update_time = #{update_time, jdbcType=VARCHAR},  <!-- 修改时间 -->
			</if>
			<if test="state != null">
		          state = #{state, jdbcType=VARCHAR},  <!-- 状态（1 有效 0 无效） -->
			</if>
		</set>
		WHERE  code_version_id = #{code_version_id}
	</update>
	
	<!-- 根据主键删除代码版本号 -->
	<delete id="deleteByKey">
		DELETE FROM bs_proj_code_version WHERE  code_version_id = #{code_version_id}
	</delete>
	
	<!-- 根据主键删除测试版本号 -->
	<delete id="deleteTestByKey">
		DELETE FROM bs_proj_test_version WHERE  code_version_id = #{code_version_id}
	</delete>
	
	<delete id="updateStopByKey">
		update bs_proj_code_version a 
		set a.state = 0 
		where a.code_version_id = #{code_version_id}
	</delete>

	<delete id="updateTestStopByKey">
		update bs_proj_test_version a 
		set a.state = 0 
		where a.code_version_id = #{code_version_id}
	</delete>

	<delete id="updateRunByKey">
		update bs_proj_code_version a 
		set a.state = 1 
		where a.code_version_id = #{code_version_id}
	</delete>
	
	<delete id="updateTestRunByKey">
		update bs_proj_test_version a 
		set a.state = 1 
		where a.code_version_id = #{code_version_id}
	</delete>
	
	<!-- 根据主键查询并返回数据持久化对象 -->
	<select id="selectByKey" resultType="ProjCodeVersionPO">
		SELECT
		<include refid="column" />
		FROM bs_proj_code_version WHERE  code_version_id = #{code_version_id}
	</select>
	
	<!-- 根据主键查询并返回数据持久化对象 -->
	<select id="selectStateByKey" resultType="ProjCodeVersionPO">
		SELECT
		state
		FROM bs_proj_code_version WHERE  code_version_id = #{code_version_id}
	</select>

	<!-- 根据唯一组合条件查询并返回数据持久化对象 -->
	<select id="selectOne" parameterType="Dto" resultType="ProjCodeVersionPO">
		SELECT
		    <include refid="column" />
		FROM bs_proj_code_version
		<where>
		    <include refid="equal" />
		</where>		
	</select>

	<!-- 根据Dto查询并返回数据持久化对象集合 -->
	<select id="list" parameterType="Dto" resultType="ProjCodeVersionPO">
		SELECT
			<include refid="column" />	
		FROM bs_proj_code_version
		<where>
		    <include refid="equal" />
		</where>	
	</select>

	<!-- 根据Dto查询并返回分页数据持久化对象集合 -->
	<select id="listPage" parameterType="Dto" resultType="ProjCodeVersionPO" useCache="false">
	    SELECT
			<include refid="column" />	
		FRoM bs_proj_code_version
		<where>
		    <include refid="equal" />
		</where>
		group by sortno
	</select>
	
	<!-- 根据Dto模糊查询并返回数据持久化对象集合(字符型字段模糊匹配，其余字段精确匹配)并集 -->
	<select id="like" parameterType="Dto" resultType="ProjCodeVersionPO">
		SELECT
			<include refid="column" />	
		FROM bs_proj_code_version
		<where>
		    <include refid="like" />
		</where>	
	</select>
	<!-- 根据Dto模糊查询并返回数据持久化对象集合(字符型字段模糊匹配，其余字段精确匹配)交集 -->
	<select id="likeOr" parameterType="Dto" resultType="ProjCodeVersionPO">
		SELECT
			<include refid="column" />	
		FROM bs_proj_code_version
		<where>
		    <include refid="likeOr" />
		</where>	
	</select>

	<!-- 根据Dto模糊查询并返回分页数据持久化对象集合(字符型字段模糊匹配，其余字段精确匹配)交集 -->
	<select id="likePage" parameterType="Dto" resultType="ProjCodeVersionPO" useCache="false">
	    SELECT
			bpcv.code_version_id,
			bpcv.code_version_number,
			bpcv.version_id,
			bpcv.test_version_id,
			bpcv.proj_id,
			bpcv.remark,
			bpcv.sortno,
			bpcv.create_id,
			bpcv.create_time,
			bpcv.update_id,
			bpcv.update_time,
			bpcv.state,
			aur.name as create_name,
			aar.name as update_name
		FRoM bs_proj_code_version bpcv
		LEFT JOIN aos_user aur ON bpcv.create_id = aur.id
		LEFT JOIN aos_user aar ON bpcv.update_id = aar.id
		<where>
		    <include refid="like" />
		</where>
		order by sortno
	</select>
	
	<select id="codeVersionNumberCount"  parameterType="Dto" resultType="Integer">
		select COUNT(code_version_id) FROM bs_proj_code_version WHERE code_version_number = #{code_version_number} AND code_version_id = #{code_version_id}
	</select>
	
	<select id="codeVersionNumberCount1"  parameterType="Dto" resultType="Integer">
		select COUNT(code_version_id) FROM bs_proj_code_version WHERE code_version_number = #{code_version_number} AND test_version_id = #{test_version_id} AND code_version_id != #{code_version_id}
	</select>
	
	<select id="selectByCodeVersinId" resultType="ProjCodeVersionPO">
		SELECT
			a.code_version_id
		FROM
			bs_proj_code_version a
		INNER JOIN qa_bug_manage c ON a.code_version_id = c.code_version_id AND a.code_version_id = #{code_version_id}
	</select>
	
	<!-- 根据Dto模糊查询并返回分页数据持久化对象集合(字符型字段模糊匹配，其余字段精确匹配)并集 -->
	<select id="likeOrPage" parameterType="Dto" resultType="ProjCodeVersionPO" useCache="false">
	    SELECT
			<include refid="column" />	
		FRoM bs_proj_code_version
		<where>
		    <include refid="likeOr" />
		</where>	
	</select>	
	
	<!-- 根据Dto统计行数 -->
	<select id="rows" resultType="Integer" parameterType="Dto">
		SELECT COUNT(1) AS aos_rows_ FROM bs_proj_code_version
		<where>
		    <include refid="equal" />
		</where>
	</select>
	
	<!-- 根据数学表达式进行数学运算 -->
	<select id="calc" parameterType="Dto" resultType="String">
		SELECT
			${_expr}
		FROM bs_proj_code_version
		<where>
		    <include refid="equal" />
		</where>
	</select>
	
	<!-- 表字段 -->
	<sql id="column">
		  code_version_id,  <!-- 代码版本号主键ID -->
		  code_version_number,  <!-- 代码版本号 -->
		  version_id,  <!-- 版本号ID -->
		  test_version_id,  <!-- 测试版本号ID -->
		  proj_id,  <!-- 项目ID -->
		  remark,  <!-- 备注 -->
		  sortno,  <!-- 排序号 -->
		  create_id,  <!-- 创建人ID -->
		  create_time,  <!-- 创建时间 -->
		  update_id,  <!-- 修改人ID -->
		  update_time,  <!-- 修改时间 -->
		  state  <!-- 状态（1 有效 0 无效） -->
	</sql>
	
	<!-- 表字段(用于外表的关联时引用) -->
	<sql id="column2">
		  bs_proj_code_version.code_version_id,  <!-- 代码版本号主键ID -->
		  bs_proj_code_version.code_version_number,  <!-- 代码版本号 -->
		  bs_proj_code_version.version_id,  <!-- 版本号ID -->
		  bs_proj_code_version.test_version_id,  <!-- 测试版本号ID -->
		  bs_proj_code_version.proj_id,  <!-- 项目ID -->
		  bs_proj_code_version.remark,  <!-- 备注 -->
		  bs_proj_code_version.sortno,  <!-- 排序号 -->
		  bs_proj_code_version.create_id,  <!-- 创建人ID -->
		  bs_proj_code_version.create_time,  <!-- 创建时间 -->
		  bs_proj_code_version.update_id,  <!-- 修改人ID -->
		  bs_proj_code_version.update_time,  <!-- 修改时间 -->
		  bs_proj_code_version.state  <!-- 状态（1 有效 0 无效） -->
	</sql>

	<!-- Where精确匹配字段 -->
	<sql id="equal">
	   <if test="code_version_id != null ">
		      AND code_version_id = #{code_version_id}  <!-- 代码版本号主键ID -->
		</if>
	    <if test="code_version_number != null and code_version_number != ''">
		      AND code_version_number = #{code_version_number}  <!-- 代码版本号 -->
		</if>
	   <if test="version_id != null ">
		      AND version_id = #{version_id}  <!-- 版本号ID -->
		</if>
		<if test="test_version_id != null ">
		      AND test_version_id = #{test_version_id}  <!-- 测试版本号ID -->
		</if>
	   <if test="proj_id != null ">
		      AND proj_id = #{proj_id}  <!-- 项目ID -->
		</if>
	    <if test="remark != null and remark != ''">
		      AND remark = #{remark}  <!-- 备注 -->
		</if>
	   <if test="sortno != null ">
		      AND sortno = #{sortno}  <!-- 排序号 -->
		</if>
	   <if test="create_id != null ">
		      AND create_id = #{create_id}  <!-- 创建人ID -->
		</if>
	    <if test="create_time != null and create_time != ''">
		      AND create_time = #{create_time}  <!-- 创建时间 -->
		</if>
	   <if test="update_id != null ">
		      AND update_id = #{update_id}  <!-- 修改人ID -->
		</if>
	    <if test="update_time != null and update_time != ''">
		      AND update_time = #{update_time}  <!-- 修改时间 -->
		</if>
	    <if test="state != null and state != ''">
		      AND state = #{state}  <!-- 状态（1 有效 0 无效） -->
		</if>
	</sql>
	
	<!-- Where模糊匹配字段 交集-->
	<sql id="like">
	    <if test="code_version_number != null and code_version_number != ''">
		      AND code_version_number like '${code_version_number}%'  <!-- 代码版本号 -->
		</if>
		<if test="version_id != null and version_id != ''">
		      AND version_id like '${version_id}%'  <!-- 版本号 -->
		</if>
		<if test="test_version_id != null and test_version_id != ''">
		      AND test_version_id like '${test_version_id}%'  <!-- 测试版本号 -->
		</if>
		<if test="proj_id != null and proj_id != ''">
		      AND proj_id like '${proj_id}%'  <!-- 项目编号 -->
		</if>
	    <if test="remark != null and remark != ''">
		      AND remark like '${remark}%'  <!-- 备注 -->
		</if>
	    <if test="create_time != null and create_time != ''">
		      AND create_time like '${create_time}%'  <!-- 创建时间 -->
		</if>
	    <if test="update_time != null and update_time != ''">
		      AND update_time like '${update_time}%'  <!-- 修改时间 -->
		</if>
	    <if test="state != null and state != ''">
		      AND state like '${state}%'  <!-- 状态（1 有效 0 无效） -->
		</if>
	</sql>
	<!-- Where模糊匹配字段 并集 -->
	<sql id="likeOr">
	    <if test="code_version_number != null and code_version_number != ''">
		      Or code_version_number like '${code_version_number}%'  <!-- 代码版本号 -->
		</if>
	    <if test="remark != null and remark != ''">
		      Or remark like '${remark}%'  <!-- 备注 -->
		</if>
	    <if test="create_time != null and create_time != ''">
		      Or create_time like '${create_time}%'  <!-- 创建时间 -->
		</if>
	    <if test="update_time != null and update_time != ''">
		      Or update_time like '${update_time}%'  <!-- 修改时间 -->
		</if>
	    <if test="state != null and state != ''">
		      Or state like '${state}%'  <!-- 状态（1 有效 0 无效） -->
		</if>
	</sql>
	
</mapper>